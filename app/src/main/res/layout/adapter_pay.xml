<?xml version="1.0" encoding="utf-8"?>

<android.support.v7.widget.CardView  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/border_accentcolor_button"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    card_view:cardCornerRadius="4dp"
    card_view:cardElevation="3dp"
    card_view:cardUseCompatPadding="true"
    card_view:cardBackgroundColor="@color/new_accent_c">



    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:background="@drawable/pay_border"
        android:layout_margin=".5dp"
        >

        <LinearLayout
            android:layout_width="@dimen/card_wallet_layout"
            android:layout_height="@dimen/card_view_height"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="0dp"

            >

            <ImageView
                android:id="@+id/image"
                android:layout_width="@dimen/card_image_width"
                android:layout_height="@dimen/card_image_width"
                android:layout_gravity="center_horizontal"
                android:scaleType="fitXY"
                card_view:srcCompat="@drawable/ic_mobile" />


            <calibrage.payzan.controls.CommonTextView
                android:id="@+id/text"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:ellipsize="none"
                android:gravity="center"
                android:padding="@dimen/txt_padding"
                android:singleLine="true"
                android:text="Sample title"
                android:textColor="@android:color/black"
                android:textSize="@dimen/txt_FontSize" />


        </LinearLayout>
    </LinearLayout>
</android.support.v7.widget.CardView>